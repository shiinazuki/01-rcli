/target
/dist
**/*.rs.bk
*.pdb

# rustc 内部错误（ICE）的转储文件。
# 两个原因让它必须单独写一行：一是 rustc 把它写在**当前工作目录**而不是 target/ 下，
# 上面那条 /target 兜不住，它会直接落在仓库根上；二是本项目默认跑 nightly，
# 撞上编译器 bug 的概率本来就比 stable 高。
rustc-ice-*.txt

# 覆盖率 / 测试报告产物
/coverage
lcov.info
junit.xml
*.profraw
*.profdata

# 性能分析产物
flamegraph.svg
perf.data*
profile.json

# 本地环境变量。最后一行是个惯例性的例外：不加的话，哪天你写了一份
# .env.example 想提交给同事参考，会被上一行悄悄忽略掉，还很难看出原因。
.env
.env.*
!.env.example

# 合并冲突残留
*.orig
*.rej

# GitLab CI 把 CARGO_HOME 指到工程目录下（只有工程目录能进 cache），
# 本地用 gitlab-runner 跑流水线时会留下这个目录
/.cargo-home

# 编辑器 / 操作系统。编辑器的本地配置一律不进版本库——各人用的编辑器不一样，
# 而且里面多半有只在自己机器上成立的路径。
.DS_Store
.idea/
.vscode/
.zed/
*.swp
*~
*.log

# 密钥不进版本库。挡的是「日常跑 rcli text generate 顺手生成、又顺手 commit」这种事。
*.sk
*.pk

# ……但 fixtures/ 下的是**测试向量**，不是秘密：它们不保护任何东西，存在的唯一目的
# 是给测试套件提供确定性输入，而且有两个的内容被测试钉死了——
#   fixtures/blake3.txt  被 src/process/text.rs 的 KNOWN_SIG 已知答案测试钉着
#   fixtures/chacha.txt  已提交的 chacha-ciphertext.txt 就是用它加密的
# 换一把密钥这两条测试就永久红了，所以必须跟着仓库走。
!/fixtures/*.sk
!/fixtures/*.pk
